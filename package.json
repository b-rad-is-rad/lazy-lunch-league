{
  "name": "lazy-lunch-league",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "yarn workspace lazy-lunch-leaguer dev",
    "build": "yarn workspaces run build",
    "lint": "yarn workspace lazy-lunch-leaguer lint",
    "storybook": "yarn workspace lazy-lunch-leaguer storybook"
  }
}
